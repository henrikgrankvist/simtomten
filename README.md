# Simtomten Cup Result calculation
This script uses Google Sheet API v4.
Go to https://developers.google.com/sheets/api/quickstart/python to enable the API and to get the required credentials.json file.